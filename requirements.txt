# Nothing here is needed to replay the recorded runs. `python broken.py` works
# on a clean Python 3.9+ with no installs and no API key.
#
# Install these to re-run against the API (--live):
anthropic>=0.109

# Optional. shared/scoring.py falls back to a difflib implementation of
# partial_ratio when rapidfuzz is absent; installing it makes the quote
# verification faster and matches the library named in the book.
rapidfuzz>=3.0
